# Speech2Text 화면 설계

기준: 2026-09-24. Swift 6, macOS 26 이상, SwiftUI + AppKit. 이 문서는 노치 오버레이(NotchOverlay.swift)와 설정 창(SettingsView.swift)을 다룬다. 음성 인식 엔진(DictationSpeech)과 텍스트 입력(TextInsertion)은 범위 밖이다.

## 참고한 자료

- Speech-to-action/DESIGN.md, Sources/SpeechToAction/NotchOverlay.swift: 어두운 캡슐, 비활성 NSPanel, 노치 안전 배치, 화면 변경 재배치, Reduce Motion과 대비 증가 처리. 가져온 것은 구조와 배치 방식이다. 명령 실행, Jev, 타이머 자동 숨김은 가져오지 않았다.
- StyleGallery `platform-guides/apple-interaction.md`: 빠른 반응, 취소·복구 가능성, 적응형 표시를 비교 질문으로 사용했다. Apple 수치를 그대로 옮기지 않는다.
- StyleGallery `platform-guides/preferences-and-accessibility.md`: 모션, 대비, 투명도, 글자 크기를 서로 다른 설정으로 본다. Reduce Motion에서도 대기 상태가 보여야 한다.
- StyleGallery `design-engineering/native-interaction-contracts.md`: 오버레이는 모달이 아니다. 배경 앱이 계속 입력을 받는다. 닫기와 취소는 서로 다른 뜻이다.
- consumer_reference: not_applicable. 제품에 쓰는 소비자 프로필 레코드가 없다.

## 원칙

1. 대상 앱의 키보드 포커스를 절대 빼앗지 않는다. 오버레이는 `.nonactivatingPanel`이고 `canBecomeKey`와 `canBecomeMain`은 false다. `orderFrontRegardless`로만 띄운다. 앱을 활성화하는 호출은 오버레이에 없다.
2. 결과는 사용자가 닫거나 다음 받아쓰기가 대체할 때까지 남는다. 타이머로 자동 숨기지 않는다.
3. 전사는 잘리지 않는다. 짧으면 한 줄이고 길면 제한된 높이 안에서 스크롤된다. 녹음 중에는 마지막 줄이 보이도록 아래쪽에 고정한다.
4. 음성 인식 결과를 흉내 내지 않는다. 엔진이 준 문자열과 상태만 표시한다. 미리 보기나 예시 전사는 만들지 않는다.
5. 입력은 확정(final) 전사만 한다. 중간 결과는 오버레이에만 보이고 대상 앱에 들어가지 않는다.

## 상태 모델

화면 상태는 형식이 정해진 값에서만 파생한다 (`DictationDisplayState`): `model.speechPhase`(SpeechPhase: idle, preparing, recording, finishing), `model.hasError`, `transcript`, `feedback`. `status`는 엔진이 이미 한국어로 만든 문구라 그대로 표시만 하고, 상태 판정에 쓰지 않는다.

| 화면 상태 | 조건 | 표시 | 조작 |
| --- | --- | --- | --- |
| 대기 | idle, 오류 없음, 전사·피드백 없음 | 마이크 아이콘, "⌃⌥D로 받아쓰기" | 설정, 닫기 |
| 연결 중 | preparing | 스피너, status | 취소 |
| 녹음 중 | recording | 빨간 점(모션 허용 시 맥박), status, 실시간 전사 | 마치기(■), 취소(✕) |
| 마무리 중 | finishing | 스피너, status, 전사 유지 | 취소 |
| 결과 | idle, 오류 없음, 전사 있음 | 체크 아이콘, status, 확정 전사, 입력 결과 피드백 | 복사, 붙여넣기, 닫기 |
| 알림 | idle, 오류 없음, 전사 없음, 피드백 있음 | 정보 아이콘, status, 피드백 | 설정, 닫기 |
| 오류 | idle, hasError | 주황 느낌표, "받아쓰기 오류", status 본문 | 설정, 닫기 (전사가 남아 있으면 복사·붙여넣기) |

## 노치 오버레이

- 모양: 어두운 불투명 표면 `Color(white: 0.11)`. 한 줄일 때는 캡슐, 전사·피드백이 있으면 모서리 반경 22의 둥근 사각형. 테두리는 흰색 12% 불투명도, 대비 증가 시 70%.
- 크기: 너비 = 화면 너비 × 0.3을 400~560으로 제한하고 좌우 여백 12를 남긴다. 한 줄 높이 44, 확장 높이 196. 전사 영역은 그 안에서 머리줄·피드백·버튼 줄을 뺀 나머지 높이를 쓰고, 넘치면 스크롤된다. 투명한 부분이 클릭을 가로채지 않도록 패널 프레임 자체를 상태에 따라 바꾼다.
- 위치: 표시 시 마우스가 있는 화면을 고른다. 위쪽 여유 = max(safeAreaInsets.top, auxiliaryTopLeft/Right 높이, 메뉴 막대 높이). 그 아래 8pt 지점에 수평 중앙 배치한다. 디스플레이 구성 변경과 스페이스 전환 시 다시 계산하고, 선택한 화면이 사라지면 주 화면으로 옮긴다.
- 레벨 `.statusBar`, 모든 스페이스와 전체 화면 보조 창에 참여한다. 앱이 비활성화돼도 숨지 않는다.
- 모션: 나타날 때 0.16초 페이드. Reduce Motion이면 페이드와 녹음 점 맥박을 끄고 정적 점을 쓴다. 상태는 아이콘과 문구로 전달하므로 모션 없이도 구분된다.
- 접근성: 상태 아이콘에 상태 이름 라벨, 전사에 식별자 `notch-transcript`, 모든 아이콘 버튼에 한국어 라벨·도움말·식별자. 상태 종류가 바뀌면 VoiceOver 알림(announcementRequested)을 보낸다. 실시간 전사 변화는 알리지 않는다.

## 설정 창

SwiftUI `Form`의 `.grouped` 스타일. 네이티브 컨트롤만 쓴다.

1. 상태: 현재 받아쓰기 상태와 마지막 피드백, 받아쓰기 시작/마무리 버튼.
2. Soniox API 키: `SecureField`. 저장(이 앱의 Keychain 서비스), 불러오기, Speech-to-action에서 가져오기. 가져오기는 버튼을 눌렀을 때만 실행되고 메모리에만 넣는다. 계속 쓰려면 저장해야 한다는 설명을 붙인다. 키가 비어 있으면 저장 버튼을 끈다.
3. 권한: 손쉬운 사용 허용 여부와 요청 버튼, 시스템 설정 열기. 마이크는 첫 녹음 때 macOS가 묻는다는 설명과 시스템 설정 열기. 앱이 다시 활성화되면 손쉬운 사용 상태를 새로 읽는다.
4. 단축키와 입력: ⌃⌥D 시작/마무리, ⌃⌥V 마지막 받아쓰기 붙여넣기. 자동 입력 토글. 확정 전사만 입력하고, 시작할 때 포커스된 입력 칸이 그대로일 때만 넣으며, 아니면 보관한다는 설명.
5. 오버레이: 받아쓰기 표시 버튼.

## 상태별 QA 체크리스트

실제 앱과 실제 키로 확인해야 한다. 아래 항목은 빌드로 증명되지 않는다.

- [ ] 실행 직후 대기 캡슐이 노치 바로 아래 중앙에 보이고, 노치 없는 외부 모니터에서는 메뉴 막대 아래에 보인다.
- [ ] 다른 앱의 텍스트 칸에 포커스를 둔 채 ⌃⌥D를 누르면 오버레이가 떠도 그 칸에 계속 타이핑할 수 있다.
- [ ] 오버레이 버튼을 클릭해도 대상 앱이 비활성화되지 않는다.
- [ ] 키 없이 시작하면 오류 상태와 엔진의 키 누락 문구가 보인다.
- [ ] 녹음 중 긴 한국어 전사가 잘리지 않고 스크롤되며, 마지막 줄이 보인다.
- [ ] ■를 누르거나 ⌃⌥D를 다시 누르면 마무리 중으로 바뀌고, 그다음 결과가 된다.
- [ ] 결과는 몇 분을 기다려도 사라지지 않고, ✕나 다음 받아쓰기로만 바뀐다.
- [ ] 복사 후 ⌘V, 붙여넣기 버튼, ⌃⌥V가 각각 동작하고 피드백이 보인다.
- [ ] 녹음·연결·마무리 중 ✕는 취소하고 "취소했어요" 피드백을 남긴다.
- [ ] 디스플레이를 연결·해제하거나 해상도를 바꾸면 오버레이가 올바른 화면 위쪽으로 옮겨진다.
- [ ] Reduce Motion과 대비 증가를 켜면 맥박과 페이드가 사라지고 테두리가 진해진다.
- [ ] VoiceOver가 오버레이 버튼 이름과 상태 변화를 읽는다.
- [ ] 설정 창: 키 저장/불러오기/가져오기 상태 문구, 손쉬운 사용 요청 후 상태 갱신, 자동 입력 토글, 큰 글자에서 잘림 없음.
