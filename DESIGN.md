# Speech2Text 화면 설계

기준: 2026-09-24. Swift 6, macOS 26 이상, SwiftUI + AppKit. 이 문서는 노치 오버레이(NotchOverlay.swift)와 설정 창(SettingsView.swift)을 다룬다. 음성 인식 엔진(DictationSpeech)과 텍스트 입력(TextInsertion)은 범위 밖이다.

## 참고한 자료

- Speech-to-action/DESIGN.md, Sources/SpeechToAction/NotchOverlay.swift: 어두운 캡슐, 비활성 NSPanel, 노치 안전 배치, 화면 변경 재배치, Reduce Motion과 대비 증가 처리. 가져온 것은 구조와 배치 방식이다. 명령 실행, Jev, 타이머 자동 숨김은 가져오지 않았다.
- StyleGallery `platform-guides/apple-interaction.md`: 빠른 반응, 취소·복구 가능성, 적응형 표시를 비교 질문으로 사용했다. Apple 수치를 그대로 옮기지 않는다.
- StyleGallery `platform-guides/preferences-and-accessibility.md`: 모션, 대비, 투명도, 글자 크기를 서로 다른 설정으로 본다. Reduce Motion에서도 대기 상태가 보여야 한다.
- StyleGallery `platform-guides/input-and-focus.md`: 단축키는 눈에 보이는 동작을 빠르게 하는 수단이다. 취소는 주인이 하나여야 한다.
- StyleGallery `design-engineering/native-interaction-contracts.md`: 오버레이는 모달이 아니다. 배경 앱이 계속 입력을 받는다. 닫기와 취소는 서로 다른 뜻이다.
- StyleGallery `design-engineering/loading-and-feedback.md`: 새 요청이 진행 중이거나 실패했을 때 남아 있는 이전 내용은 새 결과처럼 보이면 안 된다. 실패 시 복구 가능한 입력은 보존하되 이전 것이라고 표시한다. 취소 요청과 완료를 구분한다.
- StyleGallery `design-engineering/text-input-and-internationalization.md`: 한국어 전사는 줄바꿈과 길이가 예측되지 않으므로 고정 높이 대신 측정 높이와 스크롤을 쓴다.
- consumer_reference: not_applicable. 제품에 쓰는 소비자 프로필 레코드가 없다.

## 원칙

1. 대상 앱의 키보드 포커스를 절대 빼앗지 않는다. 오버레이는 `.nonactivatingPanel`이고 `canBecomeKey`와 `canBecomeMain`은 false다. `orderFrontRegardless`로만 띄운다. 오버레이 버튼은 첫 클릭에 반응하고 앱을 활성화하지 않는다. 예외는 사용자가 누른 "설정" 하나뿐이다.
2. 결과는 사용자가 닫거나 다음 받아쓰기가 대체할 때까지 남는다. 타이머로 자동 숨기지 않는다.
3. 높이는 내용이 정한다. 빈 칸을 미리 잡아 두지 않는다. 짧은 결과는 한 줄 높이만 차지하고, 긴 전사는 최대 약 5줄 높이의 영역 안에서 스크롤된다. 녹음 중에는 마지막 줄을 따라가고, 결과는 처음부터 읽히게 위쪽에 고정한다.
4. 음성 인식 결과를 흉내 내지 않는다. 엔진이 준 문자열과 상태만 표시한다.
5. 말하는 동안 시작할 때 선택한 입력 칸에 실시간으로 쓴다. 인식이 뒷부분을 고치면 이미 쓴 글자와 공통 앞부분만 남기고 바뀐 꼬리만 지우고 다시 쓴다. 입력 칸이 바뀌거나 캐럿 앞 글자가 우리가 쓴 내용과 다르면 즉시 멈추고 결과를 보관한다. 취소하면 이번에 쓴 글자만 지운다. 줄바꿈은 공백으로 바꿔 Return을 누르지 않는다.
6. 이전 받아쓰기를 현재 결과처럼 보여 주지 않는다. 연결 중·녹음 초반·오류·인식 없음 상태에서 남아 있는 텍스트는 본문이 아니라 "이전 받아쓰기" 또는 "중단 전 받은 텍스트 · 확정 아님"이라는 이름표가 붙은 보조 줄에만 나온다.

## 상태 모델

화면 상태는 형식이 정해진 값에서만 파생한다. 문구를 해석해 상태를 정하지 않는다. `status`는 엔진이 만든 한국어 문구라 그대로 표시만 한다.

입력: `model.speechPhase`(idle, preparing, recording, finishing), `model.hasError`, `transcript`, `feedback`, 그리고 `DictationSession`.

`DictationSession`은 오버레이 컨트롤러가 관찰한 단계 전환만으로 갱신하는 순수 값이다.

- idle에서 바쁜 단계로 넘어갈 때 그 순간의 전사를 `carriedTranscript`로 기억하고 결과(outcome)를 지운다. 엔진은 새 음성이 들어올 때까지 이전 전사를 그대로 두므로, 전사가 이것과 같으면 "새 텍스트 없음"으로 본다.
- 바쁜 단계에서 idle로 돌아올 때: 오류면 `failed`, 오버레이의 취소 버튼이 눌렸거나 마무리 단계(finishing)를 거치지 않았으면 `cancelled`, 그 외에는 `finished`.
- 한계: 설정 창의 취소 버튼으로 마무리 중에 취소하면 `finished`로 보일 수 있다. 같은 문장을 두 번 연속 받아쓰면 두 번째는 "새 텍스트 없음"으로 보인다. AppModel이 형식이 정해진 마지막 결과(완료/취소/실패, 빈 결과 여부)를 제공하면 이 추론을 없앤다.

| 화면 상태 | 조건 | 본문 | 보조 줄 | 머리줄 조작 | 아래 조작 |
| --- | --- | --- | --- | --- | --- |
| 대기 | idle, 오류 없음, 전사·피드백 없음 | 없음 (한 줄 캡슐) | 없음 | 시작, 설정, 닫기 | 없음 |
| 연결 중 | preparing | 없음 (이전 전사 숨김) | 없음 | 취소 | 없음 |
| 녹음 중 | recording | 이번 녹음의 실시간 전사 (없으면 한 줄 캡슐) | 없음 | 마치기(■), 취소(✕) | 없음 |
| 마무리 중 | finishing | 이번 녹음의 전사 | 없음 | 취소 | 없음 |
| 결과 | idle, finished, 새 전사 있음 | 확정 전사 | 입력 피드백 | 시작, 설정, 닫기 | 복사, 붙여넣기 ⌃⌥V |
| 알림 | idle, 새 전사 없음, 피드백 또는 이전 전사 있음 | 없음 | 이전 받아쓰기, 피드백 | 시작, 설정, 닫기 | 이전 받아쓰기 복사·붙여넣기 |
| 취소됨 | idle, cancelled | 없음 | 피드백, 녹음 전 이전 받아쓰기 | 시작, 설정, 닫기 | 이전 받아쓰기가 있을 때만 복사·붙여넣기 |
| 오류 | idle, hasError | 오류 문구 (주황) | 이전 받아쓰기 또는 중단 전 텍스트 | 닫기 | 다시 시도, 설정 확인, 남은 텍스트 복사·붙여넣기 |

취소됨 상태에서 녹음 중 받은 미확정 텍스트는 복사·붙여넣기로 제공하지 않는다.

## 노치 오버레이

- 모양: 어두운 불투명 표면 `Color(white: 0.11)`. 한 줄일 때는 캡슐(높이 44, 반경 22), 내용이 있으면 반경 18의 둥근 사각형. 테두리는 흰색 12% 불투명도, 대비 증가 시 70%.
- 너비: 한 줄 상태는 340. 내용이 있으면 화면 너비 × 0.3을 420~520으로 제한한다. 어느 쪽이든 좌우 여백 12를 남긴다.
- 높이: SwiftUI 내용을 이상적 높이로 배치해 측정하고, 패널 프레임을 그 높이로 맞춘다. 한 줄 상태는 항상 44. 전사 영역 높이 = min(측정한 전사 높이, 96)이며 넘치면 스크롤된다. 전체 높이는 화면 위쪽 여유를 뺀 높이로 제한한다. 패널 위쪽 가장자리는 상태와 관계없이 같은 자리에 있고 아래로만 늘어난다. 투명한 부분이 클릭을 가로채지 않도록 패널 자체를 내용 크기로 바꾼다.
- 위치: 표시 시 마우스가 있는 화면을 고른다. 위쪽 여유 = max(safeAreaInsets.top, auxiliaryTopLeft/Right 높이, 메뉴 막대 높이). 그 아래 8pt 지점에 수평 중앙 배치한다. 디스플레이 구성 변경과 스페이스 전환 시 다시 계산하고, 선택한 화면이 사라지면 주 화면으로 옮긴다.
- 이동: 버튼이 아닌 곳을 드래그하면 패널이 포인터를 따라 움직이고 화면 안에 머문다. 옮긴 뒤에는 위쪽 가운데 점을 기준으로 삼아, 높이가 바뀌어도 위쪽 모서리는 그대로 두고 아래로 늘어난다. 옮긴 위치는 메모리에만 두므로 앱을 다시 켜면 기본 위치에서 시작한다. 옮긴 화면이 사라져도 기본 위치로 돌아간다.
- 레벨 `.statusBar`, 모든 스페이스와 전체 화면 보조 창에 참여한다. 앱이 비활성화돼도 숨지 않는다.
- 모션: 나타날 때 0.16초 페이드, 크기 변화는 0.18초. Reduce Motion이면 페이드·크기 애니메이션·녹음 점 맥박을 끈다. 상태는 아이콘과 문구로 전달하므로 모션 없이도 구분된다.
- 접근성: 상태 아이콘에 상태 이름 라벨. 식별자: `notch-transcript`, `notch-error`, `notch-retained`, `notch-feedback`, `notch-start`, `notch-finish`, `notch-cancel`, `notch-settings`, `notch-dismiss`, `notch-retry`, `notch-open-settings`, `notch-copy`, `notch-paste`. 모든 아이콘 버튼에 한국어 라벨과 도움말. 상태 종류가 바뀌면 VoiceOver 알림을 보내고, 오류는 제목과 오류 문구를 함께 읽는다. 실시간 전사 변화는 알리지 않는다.

## 설정 창

SwiftUI `Form`의 `.grouped` 스타일, 네이티브 컨트롤만 쓴다. 긴 설명 꼬리말 대신 각 줄의 한 줄 부제를 쓴다. 최소 440×420, 권장 480×540으로 큰 빈 공간을 만들지 않는다.

1. 받아쓰기(머리글 없음): 상태 아이콘, 제목(오류면 "받아쓰기 오류", 아니면 엔진 status), 세부 문구(오류 문구는 주황·선택 가능, 아니면 마지막 피드백). 오른쪽에 주 버튼(시작/마무리)과 바쁠 때 취소. 준비 항목이 빠져 있으면 "아래 준비 항목 N개를 확인해 주세요" 줄.
2. 준비:
   - Soniox API 키: `SecureField`, 저장(이 앱의 Keychain에만), 불러오기. 다른 앱의 키는 읽지 않는다. 사용할 마이크 선택(시스템 기본값 또는 입력 장치, UserDefaults에 저장, 연결이 끊기면 시작 시 오류로 알림). 키가 비면 저장 버튼을 끄고 경고 줄을 보여 준다.
   - 마이크: `AVCaptureDevice.authorizationStatus`로 허용됨·아직 묻지 않음·거부됨을 보인다. 아직 묻지 않음이면 "허용 요청"(사용자가 누를 때만 요청), 거부됨이면 "시스템 설정 열기".
   - 다른 앱 제어 권한(`AXIsProcessTrusted`): macOS 27 이상에서는 이름을 "Device Control and Data Management(기기 제어 및 데이터 관리)"로, 26 이하에서는 "손쉬운 사용"으로 보이고 다른 버전의 이름도 함께 알려 준다. 허용 전에는 "허용 요청"과 "시스템 설정 열기". macOS 27 이름은 사용자가 알려 준 표기이며 이 문서 작성 시 직접 확인하지 않았다.
   - 앱이 다시 활성화되면 두 권한 상태를 새로 읽는다.
3. 입력: 실시간 입력 토글(시작할 때 선택한 입력 칸이 그대로일 때만, 인식 수정도 반영), ⌃⌥D 시작/마무리, ⌃⌥V 마지막 결과 붙여넣기, 화면 위쪽 표시 열기.

## 상태별 QA 체크리스트

실제 앱과 실제 키로 확인해야 한다. 아래 항목은 빌드로 증명되지 않는다.

- [ ] 실행 직후 대기 캡슐(340×44)이 노치 바로 아래 중앙에 보이고, 시작·설정·닫기 버튼이 있다. 노치 없는 외부 모니터에서는 메뉴 막대 아래에 보인다.
- [ ] 다른 앱의 텍스트 칸에 포커스를 둔 채 캡슐의 시작 버튼이나 ⌃⌥D를 누르면 그 칸에 계속 타이핑할 수 있다.
- [ ] 연결 중과 첫 단어 전에는 이전 결과가 보이지 않는다.
- [ ] 한 문장 결과는 머리줄 바로 아래에 붙어 나오고 위아래로 빈 띠가 없다.
- [ ] 녹음 중 긴 한국어 전사는 약 5줄에서 더 늘지 않고 스크롤되며 마지막 줄이 보인다. 긴 결과는 첫 줄부터 보인다.
- [ ] ■ 또는 ⌃⌥D로 마무리 중 → 결과. ✕로 취소하면 체크 표시가 아닌 취소 상태가 되고 미확정 텍스트에 복사 버튼이 없다.
- [ ] 오류 시 제목 "받아쓰기 오류", 본문이 오류 문구이고, 이전 전사는 "이전 받아쓰기" 이름표가 붙은 흐린 한 줄로만 보인다. 다시 시도와 설정 확인이 동작한다.
- [ ] 결과는 몇 분을 기다려도 사라지지 않고, ✕나 다음 받아쓰기로만 바뀐다.
- [ ] 복사 후 ⌘V, 붙여넣기 버튼, ⌃⌥V가 각각 동작하고 피드백이 보인다.
- [ ] 디스플레이를 연결·해제하거나 해상도를 바꾸면 오버레이가 올바른 화면 위쪽으로 옮겨진다.
- [ ] Reduce Motion과 대비 증가를 켜면 맥박·페이드·크기 애니메이션이 사라지고 테두리가 진해진다.
- [ ] VoiceOver가 오버레이 버튼 이름과 상태 변화를 읽는다.
- [ ] 설정 창: 480 너비에서 가로 스크롤·잘림이 없고, 키 저장/불러오기, 마이크 선택 유지, 마이크 허용 요청, 기기 제어 허용 요청 후 상태 갱신, 자동 입력 토글이 동작한다. 큰 글자에서 잘림 없음.
